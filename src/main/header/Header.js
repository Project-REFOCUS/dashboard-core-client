import React from 'react';
import HeaderMenu from './HeaderMenu';

import './Header.scss';

const Header = () => (
    <div>
        <strong className="header-title">Header component!</strong>
        <HeaderMenu />
    </div>
);

export default Header;
